[project]
name = "qwen3-32b-code-edit-input"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "datasets",
  "httpx>=0.27",
  "transformers",
  "vs-bench",
]

[tool.uv]
package = false

[tool.uv.sources]
vs-bench = { path = "../../../sdk/vs-bench" }
