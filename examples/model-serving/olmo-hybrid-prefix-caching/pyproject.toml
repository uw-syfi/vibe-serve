[project]
name = "olmo-prefix-caching-input"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27",
  "torch>=2.4",
  "transformers>=4.52",
  "fastapi>=0.110",
  "uvicorn>=0.29",
  "vs-bench",
]

[tool.uv]
package = false

[tool.uv.sources]
vs-bench = { path = "../../../sdk/vs-bench" }
