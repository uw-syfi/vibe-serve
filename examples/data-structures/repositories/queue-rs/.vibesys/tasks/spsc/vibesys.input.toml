version = 1

[agent]
domain = "generic"

[evaluator]
name = "vibesys-evaluator-queue"
version = "0.1.0"

[accuracy]
entrypoint = "vibesys-queue"
args = [
  "check", "--workspace", "${PROJECT_ROOT}",
  "--scenario", "spsc", "--operations", "32", "--trials", "100",
]
timeout_seconds = 120

[benchmark]
entrypoint = "vibesys-queue"
args = [
  "benchmark", "--workspace", "${PROJECT_ROOT}",
  "--scenario", "spsc", "--repetitions", "3",
]
timeout_seconds = 300
result_protocol = 1
