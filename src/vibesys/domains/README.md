# Domains

The domain authoring guide lives with the rest of the contributor docs:

- https://docs.vibing.systems/docs/contributing/domains
- Source: `docs/contributing/domains.md`

This pointer stays here so the guide is discoverable from the code it
describes. `pyproject.toml` still lists it as package data; that entry exists
for the old full-text README and is not needed for a pointer.
