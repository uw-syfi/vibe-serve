{{ prompt_notes }} Candidate-specific namespace override: replace the base namespace `{{ base_name }}` with `{{ candidate_name }}` everywhere the runtime contract uses it, including app, endpoint, and auxiliary resource names. This candidate override is authoritative.