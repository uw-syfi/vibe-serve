Load the model on the AMD GPU (`device="cuda"` — ROCm's PyTorch build uses the `cuda` device string) with an efficient dtype (`torch.bfloat16` or `torch.float16`). Do NOT use `float32` or CPU — inference will be far too slow.
