[build-system]
requires = ["setuptools>=70.1"]
build-backend = "setuptools.build_meta"

[project]
name = "vs-project-state"
version = "0.1.0"
description = "Portable project and run metadata for VibeSys"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "pydantic>=2",
    "vs-loop-state",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["vs_project_state*"]

[tool.setuptools.package-data]
vs_project_state = ["py.typed"]
