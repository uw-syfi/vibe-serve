Runtime instructions are at `/opt/vibesys-runtime/environment.md`. Read that file before executing, deploying, benchmarking, or profiling; it contains the authoritative environment and lifecycle rules. Candidate-specific namespace override: replace the base namespace `run-9f2a3b` with `run-9f2a3b-g2c5` everywhere the runtime contract uses it, including app, endpoint, and auxiliary resource names. This candidate override is authoritative.